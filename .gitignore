# API keys and secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Conda
.conda/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Large data files (Kaggle downloads, etc.)
data/raw/
*.csv.gz
*.jsonl.gz

# Jupyter notebooks (outputs contain raw product data; share stripped versions only)
*.ipynb

# Old experiment results (kept locally for reference, not tracked)
data/results/old_results/

# Research writing (not public until paper is ready)
*.tex
*.aux
*.log
*.pdf

# Admin / working documents (spec, notes — not public until paper is ready)
# TODO: create a proper README.md once the project is more stable and ready to share
spec.md
notes/
