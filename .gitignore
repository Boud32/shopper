# API keys and secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Conda
.conda/

figures/

# uv
.venv/
.python-version

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Large data files (Kaggle downloads, etc.)
data/raw/
*.csv.gz
*.jsonl.gz

# Jupyter notebooks (outputs contain raw product data; share stripped versions only)
*.ipynb

# Offer sets — fully regenerable from data/seed_catalog.json via generate_universe.py
data/offer_sets/

# Local-only A2 artifacts (legacy versions and first-test run)
data/architecture2/deprecated/
data/architecture2/Initial Run/

# Local-only legacy results (gemini-frontend old runs, pre-A2 gemini runs)
data/results/deprecated/
data/results/old_results/

# IDE state
.claude/

# Research writing (not public until paper is ready)
*.tex
*.aux
*.log
*.pdf

# Admin / working documents (spec, notes — not public until paper is ready)
spec.md
notes/
